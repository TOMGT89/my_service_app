# Οδηγός Εγκατάστασης AUTOVERS GT (One-Click)

Αυτός ο οδηγός θα σας βοηθήσει να εγκαταστήσετε το σύστημα τοπικά στον υπολογιστή σας με ελάχιστα βήματα.

## Προϋποθέσεις
Πριν ξεκινήσετε, διασφαλίστε ότι έχετε εγκαταστήσει:
1. **Node.js**: [Κατεβάστε το εδώ](https://nodejs.org/) (Επιλέξτε την έκδοση LTS).
2. **MongoDB**: [Κατεβάστε το Community Server εδώ](https://www.mongodb.com/try/download/community).

## Βήματα Εγκατάστασης

### 1. Αρχική Ρύθμιση (Μόνο μία φορά)
- Κάντε διπλό κλικ στο αρχείο `INSTALL-AUTOVERS.bat`.
- Το σύστημα θα ελέγξει αν έχετε Node.js και θα εγκαταστήσει αυτόματα όλα τα απαραίτητα αρχεία.
- Στο τέλος, θα δημιουργηθεί ένα εικονίδιο **AUTOVERS GT** στην επιφάνεια εργασίας σας.

### 2. Καθημερινή Χρήση
- Κάντε διπλό κλικ στο εικονίδιο **AUTOVERS GT** στην επιφάνεια εργασίας.
- Θα ανοίξει αυτόματα ο browser σας στη διεύθυνση `http://localhost:5173`.
- Το σύστημα θα τρέχει τοπικά για μέγιστη ταχύτητα.

## Λειτουργία Anti-Sleep (Render)
Το σύστημα περιλαμβάνει έναν μηχανισμό που κρατάει το online κομμάτι (Service App/Book) "ξύπνιο" όσο εσείς εργάζεστε τοπικά. Δεν χρειάζεται να κάνετε τίποτα, ξεκινάει αυτόματα με το Launch.

## Υποστήριξη
Αν αντιμετωπίσετε οποιοδήποτε πρόβλημα, βεβαιωθείτε ότι ο server της MongoDB τρέχει στον υπολογιστή σας.
